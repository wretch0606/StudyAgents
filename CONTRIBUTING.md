# StudyAgents 协作开发指南

## 1. 开工前

1. 在任务板领取或创建 Issue，写明负责人、验收条件和依赖。
2. 确认任务属于本周 MVP；范围变化由 A 记录决定。
3. 涉及跨模块字段时，先修改 `contracts/` 并与上下游确认。

## 2. 分支与提交

从最新 `main` 创建短生命周期分支：

```text
feat/<role>-<topic>
fix/<role>-<topic>
test/<role>-<topic>
docs/<role>-<topic>
```

示例：`feat/b-pdf-ingestion`、`fix/e-sse-reconnect`。

提交信息使用：

```text
<type>(<scope>): <summary>
```

常用 `type`：`feat`、`fix`、`test`、`docs`、`refactor`、`chore`。

## 3. Pull Request

- 一个 PR 解决一个明确问题，避免混合无关改动。
- 关联 Issue，并按模板写明验收证据、测试结果和契约影响。
- PR 作者先完成自查，不把明显失败交给评审人发现。
- 至少一名非主责成员批准后合并。
- 发生冲突时由 PR 作者解决并重新运行测试。
- 第 7 天只合并阻塞性修复。

固定交叉评审：

| 评审人 | 被评审模块 | 重点 |
| --- | --- | --- |
| A | E | 页面符合需求，完整演示可走通 |
| B | C | 回答、题目和评分只使用允许的证据 |
| C | D | 状态可持久化、恢复，公开/私有字段隔离 |
| D | B | 导入任务可追踪、重试和恢复 |
| E | A | 验收用例覆盖真实交互和错误提示 |

## 4. 契约优先

- 公共 JSON 使用 `snake_case`。
- 错误响应至少包含 `code`、`retryable`、`trace_id`。
- `SourceRef` 只能引用真实检索结果。
- 标准答案、评分点和私有证据不能进入公开响应或 SSE 事件。
- Schema 变更必须同时更新：

  1. JSON Schema；
  2. Mock 数据；
  3. 生产者测试；
  4. 消费者测试；
  5. 变更说明。

## 5. 测试最低要求

- B：解析、切块、RRF、页码引用和 Recall@5。
- C：Agent JSON 契约、拒答、调用上限、答案防泄露。
- D：权限、任务租约、幂等、检查点恢复、错误映射。
- E：核心页面、SSE 重连、390px/1280px、Playwright 主链路。
- A：50 条评测集、接口冒烟、演示链路和指标报告。

## 6. 每日节奏

- 开工前：15 分钟站会，同步昨天结果、今天目标和阻塞项。
- 收工前两小时：停止扩展新功能，转入合并、测试和修复。
- 当天结束前：更新 Issue/任务板，推送可运行代码。
- 第 2、4、6 天：执行集成门；不通过则次日先修主链路。

## 7. 禁止提交

- `.env`、API Key、Session Secret、数据库密码。
- 含版权或个人信息的真实课程资料。
- 标准答案、私有评分点或其他成员学习记录。
- 本机绝对路径、数据库备份、模型缓存和大型生成文件。
